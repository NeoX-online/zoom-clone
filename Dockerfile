# 1. Используем Node.js официальный образ как базовый
FROM node:18

# 2. Установка рабочей директории внутри контейнера
WORKDIR /app

# 3. Копируем файл package.json и package-lock.json в рабочую директорию
COPY package*.json ./

# 4. Установка зависимостей
RUN npm install

# 5. Копируем весь исходный код приложения в контейнер
COPY . .

# 6. Пробрасываем порт для работы приложения
EXPOSE 3000

# 7. Запускаем команду для старта сервера
CMD ["node", "server.js"]